# ระบบบริหารจัดการแข่งขันกีฬากลุ่มโรงเรียนสว่างสูงกระสัง ประจำปีการศึกษา 2569

ระบบเว็บแอปพลิเคชันบริหารจัดการแข่งขันกีฬา ลงทะเบียนนักกีฬา บันทึกคะแนนสด สรุปเหรียญรางวัล และออกเกียรติบัตรพร้อม QR Code ตรวจสอบความถูกต้อง รองรับสถาปัตยกรรม **PHP 8.x + MySQL 8.x (PDO)** 

---

## 📁 โครงสร้างโปรเจกต์ (Directory Structure)

```text
├── config/
│   └── database.php         # คลาส PDO Database เชื่อมต่อ MySQL และมีระบบ Auto-Installer
├── includes/
│   ├── auth.php             # ระบบตรวจสอบสิทธิ์ Role-Based Access Control (RBAC)
│   ├── header.php           # ส่วนหัว Responsive Navbar, Prompt/Kanit font และ Tailwind Assets
│   └── footer.php           # ส่วนท้ายเว็บไซต์
├── admin/
│   ├── index.php            # แผงควบคุมผู้ดูแลระบบ (Admin Dashboard)
│   └── schools.php          # จัดการข้อมูล 12 โรงเรียน และรีเซ็ตรหัสผ่าน SMIS (123456)
├── school/
│   └── index.php            # แผงควบคุมสำหรับโรงเรียน (ส่งนักกีฬา / ตรวจสอบสถานะ)
├── judge/
│   └── index.php            # แผงบันทึกผลการแข่งขันและเหรียญรางวัลสำหรับกรรมการผู้ตัดสิน
├── api/
│   └── results.php          # RESTful JSON Endpoint สรุปคะแนนและเหรียญรางวัลสด
├── index.php                # หน้าแรก Public Portal & ตารางสรุปเหรียญรางวัลรวม 12 โรงเรียน
├── install.php              # Web Auto-Installer สร้างฐานข้อมูลและนำเข้าข้อมูลตั้งต้นใน 1 คลิก
├── login.php                # หน้าเข้าสู่ระบบ (โรงเรียนใช้รหัส SMIS 8 หลัก)
├── logout.php               # ออกจากระบบและทำลาย Session
├── change-password.php      # บังคับเปลี่ยนรหัสผ่านครั้งแรกเมื่อเข้าสู่ระบบด้วยรหัสตั้งต้น
├── verify.php               # หน้าตรวจสอบความถูกต้องของเกียรติบัตรผ่าน QR Token / รหัสเกียรติบัตร
├── database.sql             # MySQL Schema ครบทั้ง 13 ตาราง พร้อมข้อมูลโรงเรียนและชนิดกีฬา
├── .htaccess                # การตั้งค่า Apache (UTF-8, Security Headers, Deny Sensitive Files)
└── README.md                # คู่มือการติดตั้งและคำอธิบายระบบ
```

---

## 🏫 รายชื่อโรงเรียนในกลุ่มสว่างสูงกระสัง และบัญชีผู้ใช้งาน (SMIS Login)

| ลำดับ | รหัส SMIS (Username) | ชื่อโรงเรียน | รหัสผ่านตั้งต้น | สถานะบังคับเปลี่ยนรหัส |
|:---:|:---:|:---|:---:|:---:|
| 1 | `31030064` | โรงเรียนบ้านหนองหว้า | `123456` | บังคับเปลี่ยนเมื่อ Login ครั้งแรก |
| 2 | `31030059` | โรงเรียนบ้านโคกสว่าง | `123456` | บังคับเปลี่ยนเมื่อ Login ครั้งแรก |
| 3 | `31030066` | โรงเรียนบ้านโคกสูงคูขาด | `123456` | บังคับเปลี่ยนเมื่อ Login ครั้งแรก |
| 4 | `31030081` | โรงเรียนบ้านบุกระสัง | `123456` | บังคับเปลี่ยนเมื่อ Login ครั้งแรก |
| 5 | `31030060` | โรงเรียนบ้านโคกลอย | `123456` | บังคับเปลี่ยนเมื่อ Login ครั้งแรก |
| 6 | `31030083` | โรงเรียนบ้านสระสะแก | `123456` | บังคับเปลี่ยนเมื่อ Login ครั้งแรก |
| 7 | `31030082` | โรงเรียนบ้านหนองมัน | `123456` | บังคับเปลี่ยนเมื่อ Login ครั้งแรก |
| 8 | `31030061` | โรงเรียนบ้านตะกรุมทอง | `123456` | บังคับเปลี่ยนเมื่อ Login ครั้งแรก |
| 9 | `31030062` | โรงเรียนบ้านโนนพะไล | `123456` | บังคับเปลี่ยนเมื่อ Login ครั้งแรก |
| 10 | `31030065` | โรงเรียนบ้านสระตะเคียน | `123456` | บังคับเปลี่ยนเมื่อ Login ครั้งแรก |
| 11 | `31030067` | โรงเรียนมิตรภาพโนนสมบูรณ์ | `123456` | บังคับเปลี่ยนเมื่อ Login ครั้งแรก |
| 12 | `31030063` | โรงเรียนบ้านสะเดาหวาน | `123456` | บังคับเปลี่ยนเมื่อ Login ครั้งแรก |

### บัญชีแอดมินและกรรมการกลาง
- **Super Admin**: `superadmin` / `admin1234`
- **Admin การแข่งขัน**: `admin` / `admin1234`
- **กรรมการผู้ตัดสิน**: `referee1` / `judge1234`

---

## 🚀 ขั้นตอนการติดตั้งบนเซิร์ฟเวอร์ PHP/MySQL

### วิธีที่ 1: ติดตั้งผ่านหน้าเว็บอัตโนมัติ (Web Installer - แนะนำและสะดวกที่สุด)
1. อัปโหลดไฟล์ทั้งหมดขึ้นเซิร์ฟเวอร์ Apache/Nginx (DirectAdmin, cPanel, หรือ XAMPP ในโฟลเดอร์ `htdocs` หรือ `public_html`)
2. สร้างฐานข้อมูลและ User ผ่าน Control Panel ของโฮสติ้งตามที่คุณต้องการ (เช่น `schoolos12_sports`, User `schoolos12_sawang`)
3. เปิดเบราว์เซอร์ไปที่ `http://your-domain.com/install.php`
4. กรอกข้อมูลการเชื่อมต่อ MySQL ของคุณในช่องรับข้อมูล:
   - **Database Host**: เช่น `localhost` หรือ `127.0.0.1`
   - **Database Port**: เช่น `3306`
   - **ชื่อฐานข้อมูล (Database Name)**: เช่น `schoolos12_sports` (ชื่อฐานข้อมูลที่คุณสร้างไว้)
   - **Database Username**: เช่น `schoolos12_sawang`
   - **Database Password**: รหัสผ่านฐานข้อมูลของคุณ
5. กดปุ่ม **"🚀 บันทึกการตั้งค่า & เริ่มการติดตั้งฐานข้อมูลทันที"**
6. ระบบจะทดสอบเชื่อมต่อ, นำเข้าโครงสร้างตาราง 13 ตาราง, ข้อมูลโรงเรียน 12 แห่ง และบันทึกค่าลงไฟล์ `config/database.php` ให้อัตโนมัติทันที 100%

### วิธีที่ 2: นำเข้าผ่าน phpMyAdmin (สำหรับ Shared Web Hosting / DirectAdmin / cPanel)
1. สร้างฐานข้อมูลผ่าน Control Panel ของโฮสติ้ง (เช่น `schoolos12_sports` หรือชื่อที่ต้องการ) และผูกสิทธิ์ Database User
2. เปิด **phpMyAdmin** แล้วคลิกเลือก **ชื่อฐานข้อมูลของคุณในแถบซ้ายมือ** ให้หน้าจออยู่ที่ฐานข้อมูลนั้น
3. คลิกแท็บ **"นำเข้า" (Import)** ด้านบน
4. เลือกไฟล์ `database.sql` แล้วกด **"นำเข้า" (Import/Go)**
5. ไฟล์ `database.sql` ได้รับการปรับปรุงให้ไม่มีคำสั่ง `CREATE DATABASE` แล้ว จึงสามารถนำเข้าลงในชื่อฐานข้อมูลใดๆ บน Web Hosting ได้อย่างราบรื่น 100% ไม่ติดข้อผิดพลาด `#1044 Access Denied`
